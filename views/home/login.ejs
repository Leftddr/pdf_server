<!DOCTYPE html>
<html>

<head>
  <%- include('../partials/head') %>
  <style>
    html,
    body {
      background-color: #eee;
    }

    ,
  </style>
</head>

<body>


  <div class="row">
    <div class="col-md-8">
      <img src="images/Jonathan.png"/>
    </div>

    <div class="col-md-4">
      <h1 class="mb-3 inner">Login</h3>
        <h3 class="mb-3 small_inner">Jonathan is waiting for you!</h2>

          <form class="user-form big_inner" action="/login" method="post">
            <div class="form-group row">
              <label for="username" class="col-sm-3 col-form-label">Username</label>
              <div class="col-sm-9">
                <input type="text" id="username" name="username" value="<%= username %>" class="form-control <%= (errors.username)?'is-invalid':'' %>">
                <% if(errors.username){ %>
                <span class="invalid-feedback"><%= errors.username %></span>
                <% } %>
              </div>
            </div>
            <div class="form-group row">
              <label for="password" class="col-sm-3 col-form-label">Password</label>
              <div class="col-sm-9">
                <input type="password" id="password" name="password" value="" class="form-control <%= (errors.password)?'is-invalid':'' %>">
                <% if(errors.password){ %>
                <span class="invalid-feedback"><%= errors.password %></span>
                <% } %>
              </div>
            </div>
            <% if(errors.login){ %>
            <div class="invalid-feedback d-block"><%= errors.login %></div>
            <% } %>
            <div class="mt-3 small_inner">
              <input class="btn btn-primary btn-lg btn-block" type="submit" value="Submit">
              <div class="small_inner">
                <h5 style="display:inline">Don't have an account yet? </h5><a href="/users/new" style="font-size:25px">
                  <strogn><u>Sign In!</u></strong>
                </a>
              </div>
            </div>
          </form>

    </div>
  </div>
</body>

</html>
